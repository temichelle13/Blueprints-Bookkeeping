{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check:indexing-guards": "tsx ./src/check-indexing-guards.ts",
    "check:merge-conflicts": "tsx ./src/check-merge-conflicts.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "check:route-references": "tsx ./src/check-route-references.ts"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:"
  }
}
